baseURL = 'https://mikeinnes.io/'
languageCode = 'en-gb'
title = 'Machine Lines'
disableKinds = ["taxonomy", "term"]

# for image tags
[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
    style = 'monokai'

[outputFormats]
  [outputFormats.RSS]
    baseName = "feed"
